name: ye_student_tutor


# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+6

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
    
  
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  uuid: ^4.5.2
  markdown: ^7.0.0
  flutter_markdown: ^0.6.18
  flutter_math_fork: ^0.7.1 
  webview_flutter: ^4.4.2
  cached_network_image: ^3.3.0
  flutter_tex: ^4.0.9
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  url_launcher: ^6.2.4
  flutter_secure_storage: ^9.2.2   # تخزين آمن (Keychain/Keystore) للمعرّفات الحساسة
  speech_to_text: ^7.0.0           # 🎤 تحويل الصوت لنص على الجهاز (يدير أذوناته بنفسه)
  image_picker: ^1.1.2             # 📷 التقاط صورة من الكاميرا أو المعرض
  flutter_image_compress: ^2.3.0   # ضغط الصورة قبل الرفع (شبكات ضعيفة)
  image_cropper: ^8.0.2            # ✂️ قص الصورة قبل الإرسال
  flutter_launcher_icons: ^0.13.1  # تأكد من استخدام أحدث إصدار
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1
  cloud_firestore: ^6.9.0
  google_sign_in: ^7.2.0
  # 🔔 إشعارات الدفع. يلزمها إعدادٌ أصليّ لا يكفي فيه الاعتماد:
  #    أندرويد ← إذن POST_NOTIFICATIONS وقناة `masar_general` (في المانيفست)،
  #    iOS ← قدرة Push Notifications ومفتاح APNs في Firebase Console.
  #    راجع docs/06-admin.md — بلا مفتاح APNs لا يصل إشعارٌ واحد لآيفون.
  firebase_messaging: ^16.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # 🔒 إيقاف Swift Package Manager لهذا المشروع (2026-08-29)
  # ─────────────────────────────────────────────────────────
  # فلاتر يولّد `ios/Flutter/ephemeral/.../Package.swift` مُعلناً
  # `.iOS("13.0")`، بينما `cloud_firestore` و`firebase_auth` يتطلبان 15.0،
  # فيسقط بناء Xcode بـ:
  #   «The package product 'cloud-firestore' requires minimum platform
  #    version 15.0 … but this target supports 13.0»
  # وحذف الملف يدوياً **لا يكفي**: أي `flutter pub get` يعيد توليده بـ13.0.
  # وإطفاء SPM يمرّر كل الإضافات عبر CocoaPods — و`Podfile` مضبوط على 15.0
  # و`post_install` فيه يرفع ولا يخفض. راجع [docs/32-scholarships.md §12].
  config:
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # 🖼️ الأصول (الشعار الرسمي يُستخدم في السبلاش والتوثيق)
  assets:
    - assets/icon/icon.png
    - assets/brand/logo_mark.png
    - assets/brand/logo_full.png
    - assets/brand/robot_face.png
    - assets/brand/robot_fly.png
    - assets/art/art_analysis.png
    - assets/art/art_checklist.png
    - assets/art/art_services.png
    - assets/art/art_scholarship.png
    - assets/art/art_student_info.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  fonts:
    # ✒️ أيقونات Phosphor — مكتبة المصمّم. أربعة أنماطٍ فقط ممّا يستعمله
    #    التصميم؛ وباقي أنماط المكتبة لم تُنسخ لأنها وزنٌ بلا استعمال.
    - family: Phosphor
      fonts:
        - asset: assets/fonts/Phosphor.ttf
    - family: PhosphorFill
      fonts:
        - asset: assets/fonts/Phosphor-Fill.ttf
    - family: PhosphorBold
      fonts:
        - asset: assets/fonts/Phosphor-Bold.ttf
    # 🎨 **الثنائيّ (Duotone)** — نمطُ رأس الشاشة في التصميم: طبقةٌ باهتة
    #    تحت الخطوط. قِستُه من التصدير: المصباح والقائمة وفاضل الإعدادات
    #    كلُّها ذاتُ ظِلٍّ داخليّ فاتح، وهذا لا يُصنع بنمطٍ ممتلئ ولا عادي.
    - family: PhosphorDuotone
      fonts:
        - asset: assets/fonts/Phosphor-Duotone.ttf

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png" # مسار صورتك
  min_sdk_android: 21 # اختياري