# 10 — المخاطر والديون التقنية

## 🔴 عالية الخطورة

### R-01 — فهرس FAISS يُبنى عند الطلب ولا يُحفظ
`common.py:197` — مهلة بناء 40 ثانية، سيمافور 2، كاش ذاكرة فقط. HF ينام → كل شيء يُبنى من الصفر. **الفرونت مهلته 60 ثانية.**
→ [13§2](13-performance.md) · [ADR-012](08-decisions.md)

### R-02 — الوزاري يرجع أسئلة من وحدات أخرى
`filter_and_rank_exams` عتبتها **كلمة واحدة**، والأسئلة غير موسومة بالدرس.
→ [12](12-wazari-accuracy.md) · [ADR-007](08-decisions.md)

### R-03 — صيغتان متعارضتان للكتب
الأحياء بلا دروس، الباقي بلا صفحات. **"وضعان لكل مادة" مستحيل اليوم.**
→ [11§2](11-content-schema.md) · [ADR-011](08-decisions.md)

### R-04 — وزاري عربي وإنجليزي معطّل بصمت
صيغتهما (`الأقسام` / `أقسام_الأسئلة`) لا يقرؤها `collect_exam_questions_by_years` → **صفر أسئلة، بلا رسالة خطأ**.

### R-05 — لا خطة ترحيل للـ 2,500 مستخدم
من نظام الأكواد إلى Firebase Auth. غير مذكورة في v3 إطلاقاً.

## 🟠 متوسطة

### R-06 — بصمة FAISS قابلة للتصادم
`f"{len(texts)}_{texts[0][:80]}_{texts[-1][:80]}"` — نصّان بنفس الطول والحدود → **فهرس خاطئ بصمت**. الحل: هاش المحتوى كاملاً.

### R-07 — `normalize_arabic` معرّفة مرتين
`common.py:301` و`:489`. الثانية تطغى على الأولى. مربك وخطر عند التعديل.

### R-08 — ديمو وإنتاج في نفس التطبيق
`features/future_masar/` (22 شاشة ببيانات ثابتة) داخل تطبيق الإنتاج. خطر شحن شاشة ديمو للطلاب.

### R-09 — `flutter_markdown` مهجورة
تعرض كل ردود الـ AI.

### R-10 — كود وملفات ميتة
`filter_exams_smart` · `filter_exams_by_keyword` · `book.index` · `book_docs.json` · `book_meta.json` · `exam_embeddings.npz` · `codes.json`

### R-11 — HF Spaces تنام
v3 §3.6 تبني على أن إعادة التشغيل "نادرة" — غير صحيح.

## 🟡 منخفضة
- **R-12** — لا كاش للكتب؛ `load_json_safe` يعيد التحليل في كل طلب
- **R-13** — موديل التضمين يُنزَّل وقت التشغيل بدل تثبيته في صورة Docker
- **R-14** — `QA_TOP_K = 3` منخفض
- **R-15** — ثلاث مكتبات معادلات (`flutter_tex` تشغّل WebView — ثقيلة على أجهزة 2GB)
- **R-16** — `build_runner`/`hive_generator` في `dependencies`
- **R-17** — لا اختبارات ولا CI
- **R-18** — لا معالجة صريحة لـ 429
- **R-19** — `اسلامية` و`قرآن` مجلدات فارغة (0 B) لكنها موجودة في البنية
