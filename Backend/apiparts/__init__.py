# -*- coding: utf-8 -*-
"""📦 أجزاءُ [api.py].

⚠️ **تُستورد من `api` وحده وفي ترتيبها**: كلُّ جزءٍ يسجّل مساراتِه على
   `app` لحظةَ استيراده، ويأخذ ما يحتاجه بـ`from api import …` بينما
   `api` نصفُ مُنفَّذ. فاستيرادُ جزءٍ ابتداءً يقلب الترتيبَ.
"""

# 🔒 **وأيّاً كان المدخل، `api` هو البادئ.**
#
# ☢️ بدون هذا السطر كان `import apiparts.ingest` ابتداءً يقلب السلسلة:
#    يستورد `api`، فيستورد `api` الجزءَ نفسَه وهو نصفُ مُنفَّذ، فيسقط
#    بـ`cannot import name … from partially initialized module`. وهو عطلٌ
#    لا يظهر في التشغيل العاديّ (`uvicorn api:app`) بل عند أوّل من يستورد
#    جزءاً مباشرةً — أداةً كانت أو اختباراً.
import api  # noqa: F401,E402
